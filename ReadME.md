# 🤖 Gemini Telegram Bot

[![Quality Gate Status](https://sonarcloud.io/api/project_badges/measure?project=mplay89_botllm&metric=alert_status&token=b5aefd416f7ac1d4c24d4b9c451812257cc5660b)](https://sonarcloud.io/summary/new_code?id=mplay89_botllm)
[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)](https://www.python.org/downloads/)
[![Aiogram 3.x](https://img.shields.io/badge/aiogram-3.x-blue.svg)](https://docs.aiogram.dev/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

Інтелектуальний Telegram-бот на базі Google Gemini AI з підтримкою текстових запитів, голосових повідомлень, обробки зображень та адміністративної панелі.

## 📋 Зміст

- [Особливості](#-особливості)
- [Технології](#️-технології)
- [Архітектура](#-архітектура)
- [Встановлення](#-встановлення)
- [Конфігурація](#️-конфігурація)
- [Запуск](#-запуск)
- [Команди бота](#-команди-бота)
- [Структура проєкту](#-структура-проєкту)
- [Тестування](#-тестування)
- [Docker](#-docker)
- [Розробка](#-розробка)

## ✨ Особливості

### 🎯 Основний функціонал

- **🤖 Google Gemini AI** - Інтеграція з найновішими моделями Gemini
- **💬 Текстові запити** - Обробка будь-яких текстових повідомлень з контекстом
- **🗣️ Голосові повідомлення** - Розпізнавання та відповідь голосом (TTS)
- **🖼️ Обробка зображень** - Аналіз та опис зображень через Gemini Vision
- **📝 Довгі відповіді** - Автоматичне розбиття довгих текстів на частини
- **💾 Контекст розмови** - Збереження історії діалогу для кожного користувача

### 👑 Адміністрування

- **👥 Управління адмінами** - Додавання/видалення адміністраторів (тільки власник)
- **🤖 Зміна моделі AI** - Вибір моделі Gemini через інтерфейс
- **ℹ️ Моніторинг кешу** - Відстеження стану кешів системи
- **📊 Логування** - Детальне логування всіх операцій

### ⚙️ Налаштування користувача

- **🗣️ Голос TTS** - Вибір чоловічого/жіночого голосу
- **✅ Увімкнення/Вимкнення TTS** - Керування озвучуванням відповідей
- **🗑️ Очищення контексту** - Видалення історії розмови

## 🛠️ Технології

### Backend

- **Python 3.11+** - Основна мова програмування
- **Aiogram 3.x** - Асинхронний фреймворк для Telegram Bot API
- **Google Gemini API** - AI модель для генерації відповідей
- **PostgreSQL** - База даних (через Neon.tech)
- **asyncpg** - Асинхронний драйвер PostgreSQL

### AI & ML

- **google-genai** - Офіційний Python SDK для Gemini
- **Vosk** - Розпізнавання голосу (Speech-to-Text)
- **Edge-TTS** - Синтез мовлення (Text-to-Speech)

### Utilities

- **cryptography** - Шифрування конфіденційних даних
- **Pillow** - Обробка зображень
- **aiofiles** - Асинхронна робота з файлами
- **pydantic-settings** - Управління налаштуваннями

### DevOps & Tools

- **Docker & Docker Compose** - Контейнеризація
- **pytest** - Тестування
- **pytest-asyncio** - Асинхронні тести
- **pytest-cov** - Покриття коду
- **ruff** - Лінтер та форматер
- **SonarCloud** - Аналіз якості коду

## 📁 Архітектура

Проєкт побудований на основі чистої архітектури з розділенням відповідальності:

```
bot/
├── app.py                  # Точка входу, ініціалізація бота
├── config/                 # Налаштування (Pydantic Settings)
├── core/                   # Інфраструктура (логування)
├── db/                     # Database Layer
│   ├── database.py         # Підключення до БД
│   ├── cache.py            # In-memory кешування
│   ├── user_settings.py    # Налаштування користувачів
│   ├── admin_store.py      # Управління адмінами
│   ├── config_store.py     # Конфігурація бота
│   └── model_store.py      # Доступні моделі AI
├── handlers/               # Обробники команд і повідомлень
│   ├── general.py          # Загальні команди (/start, текст)
│   ├── admin.py            # Адмін-панель
│   └── settings.py         # Налаштування користувача
├── services/               # Бізнес-логіка
│   └── gemini.py           # Сервіс роботи з Gemini AI
└── presentation/           # UI Layer
    ├── keyboards/          # Клавіатури (inline, reply)
    ├── message_utils.py    # Утиліти для повідомлень
    └── status_messages.py  # Статус-повідомлення
```

### Шари архітектури:

1. **Presentation Layer** (`presentation/`) - UI компоненти, клавіатури, форматування
2. **Handlers Layer** (`handlers/`) - Обробники Telegram подій
3. **Service Layer** (`services/`) - Бізнес-логіка, інтеграція з AI
4. **Database Layer** (`db/`) - Робота з даними, кешування
5. **Core Layer** (`core/`) - Інфраструктура (логування, помилки)

## 🚀 Встановлення

### Вимоги

- Python 3.11 або вище
- PostgreSQL (або використовуйте [Neon.tech](https://neon.tech/) безкоштовно)
- Telegram Bot Token (отримайте у [@BotFather](https://t.me/botfather))
- Google Gemini API Key (отримайте на [AI Studio](https://aistudio.google.com/))

### Крок 1: Клонування репозиторію

```bash
git clone https://github.com/mplay89/botllm.git
cd botllm
```

### Крок 2: Встановлення залежностей

```bash
# Створення віртуального середовища
python -m venv venv

# Активація
# Windows:
venv\Scripts\activate
# Linux/MacOS:
source venv/bin/activate

# Встановлення залежностей
pip install -r requirements.txt

# Для розробки (опціонально)
pip install -r requirements-dev.txt
```

## ⚙️ Конфігурація

Створіть файл `.env` у корені проєкту:

```env
# Telegram Bot Token (обов'язково)
TG_TOKEN=your_telegram_bot_token

# Google Gemini API Key (обов'язково)
GEMINI_API_KEY=your_gemini_api_key

# Owner User ID - ваш Telegram ID (обов'язково)
# Отримайте у @userinfobot
OWNER_ID=123456789

# PostgreSQL Database URL (обов'язково)
DATABASE_URL=postgresql://user:password@host:port/database
```

### Отримання Telegram ID

1. Відкрийте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Натисніть `/start`
3. Скопіюйте ваш `ID` та вставте в `OWNER_ID`

### База даних

Проєкт підтримує PostgreSQL. Рекомендуємо використовувати безкоштовний хостинг:

- **[Neon.tech](https://neon.tech/)** - Serverless PostgreSQL (безкоштовно 0.5GB)
- **[Supabase](https://supabase.com/)** - PostgreSQL + Backend (безкоштовно 500MB)
- **[ElephantSQL](https://www.elephantsql.com/)** - PostgreSQL as a Service (безкоштовно 20MB)

## 🏃 Запуск

### Локальний запуск

```bash
# Активувати віртуальне середовище
source venv/bin/activate  # Linux/MacOS
# або
venv\Scripts\activate  # Windows

# Запустити бота
python -m bot.app
```

### Docker (рекомендовано)

```bash
# Запуск усіх сервісів
docker-compose up -d

# Перегляд логів
docker-compose logs -f bot

# Зупинка
docker-compose down
```

### Health Check

Бот включає health check для перевірки стану:

```bash
python health_check.py
```

Вихідний код `0` = бот працює, `1` = помилка.

## 📱 Команди бота

### Користувацькі команди

| Команда | Опис |
|---------|------|
| `/start` | Запуск бота, показує головне меню |
| Текст | Будь-який текст відправляється в Gemini AI |
| Голос | Голосові повідомлення розпізнаються та обробляються |
| Зображення | Зображення аналізуються Gemini Vision |

### Кнопки меню

**Головне меню:**
- `⚙️ Налаштування` - Відкрити налаштування
- `👑 Адмін-панель` - Відкрити адмін-панель (тільки для адмінів)

**Меню налаштувань:**
- `🗣️ Голос (Чоловічий/Жіночий)` - Зміна голосу TTS
- `✅ Увімкнути TTS / ❌ Вимкнути TTS` - Перемикач озвучування
- `🗑️ Очистити контекст` - Видалити історію розмови
- `⬅️ Назад до головного меню` - Повернутись назад

**Адмін-панель (тільки для адмінів):**
- `🤖 Змінити модель AI` - Вибір моделі Gemini
- `ℹ️ Інфо про кеш` - Експорт звіту про кеш (файл .txt)
- `👥 Редагувати адмінів` - Управління адмінами (тільки власник)
  - `➕ Додати адміна` - Додати нового адміна
  - `➖ Видалити адміна` - Видалити адміна
  - `📋 Список адмінів` - Показати всіх адмінів

## 📂 Структура проєкту

```
botllm/
├── bot/                        # Основний пакет бота
│   ├── __init__.py
│   ├── app.py                  # Точка входу
│   ├── config/                 # Налаштування
│   │   ├── __init__.py
│   │   └── settings.py         # Pydantic Settings
│   ├── core/                   # Інфраструктурний шар
│   │   ├── __init__.py
│   │   └── logging_setup.py    # Налаштування логування
│   ├── db/                     # Database Layer
│   │   ├── __init__.py
│   │   ├── database.py         # Підключення до PostgreSQL
│   │   ├── cache.py            # In-memory кешування
│   │   ├── user_settings.py    # Дані користувачів
│   │   ├── admin_store.py      # Адміністратори
│   │   ├── config_store.py     # Конфігурація бота
│   │   ├── model_store.py      # Моделі AI
│   │   └── encrypted_store.py  # Шифровані дані
│   ├── handlers/               # Telegram Handlers
│   │   ├── __init__.py
│   │   ├── general.py          # Загальні команди
│   │   ├── admin.py            # Адмін-панель
│   │   └── settings.py         # Налаштування
│   ├── services/               # Бізнес-логіка
│   │   ├── __init__.py
│   │   └── gemini.py           # Gemini AI Service
│   └── presentation/           # UI Layer
│       ├── __init__.py
│       ├── keyboards/          # Telegram клавіатури
│       │   ├── __init__.py
│       │   ├── inline.py       # Inline клавіатури
│       │   └── reply.py        # Reply клавіатури
│       ├── message_utils.py    # Утиліти для повідомлень
│       └── status_messages.py  # Статус-повідомлення
├── tests/                      # Тести
│   ├── data/                   # Тести Database Layer
│   ├── handlers/               # Тести Handlers
│   ├── services/               # Тести Services
│   ├── utils/                  # Тести Utilities
│   ├── conftest.py             # Pytest конфігурація
│   └── test_health_check.py    # Тести Health Check
├── .github/                    # GitHub Actions CI/CD
├── docker-compose.yml          # Docker Compose конфігурація
├── Dockerfile                  # Docker образ
├── requirements.txt            # Залежності
├── requirements-dev.txt        # Залежності для розробки
├── pytest.ini                  # Pytest налаштування
├── .coveragerc                 # Coverage налаштування
├── sonar-project.properties    # SonarCloud налаштування
├── health_check.py             # Health Check скрипт
├── CHANGELOG.md                # Історія змін
└── README.md                   # Ця документація
```

## 🧪 Тестування

### Запуск всіх тестів

```bash
# Активувати віртуальне середовище
source venv/bin/activate

# Запустити pytest
pytest

# З детальним виводом
pytest -v

# З покриттям коду
pytest --cov=bot --cov-report=html

# Відкрити звіт покриття
# Windows:
start htmlcov/index.html
# Linux:
xdg-open htmlcov/index.html
# MacOS:
open htmlcov/index.html
```

### Запуск конкретних тестів

```bash
# Тести для конкретного модуля
pytest tests/services/test_gemini.py

# Тести для конкретної функції
pytest tests/services/test_gemini.py::test_generate_text_response

# Тести з маркером
pytest -m unit
```

### Покриття коду

Поточне покриття коду: **~85%**

Детальний звіт: `htmlcov/index.html` після запуску `pytest --cov`

## 🐳 Docker

### Структура Docker

```yaml
services:
  bot:                    # Telegram бот
    build: .
    env_file: .env
    restart: on-failure
    volumes:
      - .:/app
    
  test:                   # Тестування
    build: .
    env_file: .env
    command: pytest -v
    
  autoheal:               # Автоматичне відновлення
    image: willfarrell/autoheal
```

### Основні команди Docker

```bash
# Збірка образу
docker-compose build

# Запуск
docker-compose up -d

# Логи
docker-compose logs -f bot

# Зупинка
docker-compose down

# Перезапуск
docker-compose restart bot

# Запуск тестів
docker-compose run --rm test

# Очищення
docker-compose down -v
```

## 💻 Розробка

### Встановлення dev залежностей

```bash
pip install -r requirements-dev.txt
```

### Code Style

Проєкт використовує **Ruff** для лінтингу та форматування:

```bash
# Перевірка коду
ruff check .

# Автоматичне виправлення
ruff check --fix .

# Форматування
ruff format .
```

### Pre-commit hooks (опціонально)

```bash
# Встановлення pre-commit
pip install pre-commit

# Налаштування хуків
pre-commit install

# Запуск вручну
pre-commit run --all-files
```

### Додавання нових функцій

1. Створіть нову гілку: `git checkout -b feature/new-feature`
2. Напишіть код у відповідному шарі архітектури
3. Додайте тести до `tests/`
4. Переконайтесь що тести проходять: `pytest`
5. Перевірте code style: `ruff check .`
6. Закомітьте: `git commit -m "Add new feature"`
7. Створіть Pull Request

### Структура тестів

```python
# tests/services/test_gemini.py
import pytest
from unittest.mock import AsyncMock
from bot.services.gemini import GeminiService

@pytest.mark.asyncio
async def test_generate_text_response():
    service = GeminiService(user_id=123, bot=AsyncMock())
    response = await service.generate_text_response("Hello")
    assert isinstance(response, str)
    assert len(response) > 0
```

## 📊 Моніторинг та логування

### Логи

Бот веде детальне логування всіх операцій:

```python
# Логи зберігаються у форматі:
[2024-11-15 12:00:00] INFO - Користувач John (ID: 123) запустив бота.
[2024-11-15 12:00:15] INFO - Надіслано відповідь від Gemini для користувача (ID: 123).
```

### Рівні логування

- **DEBUG** - Детальна діагностична інформація
- **INFO** - Загальна інформація про роботу
- **WARNING** - Попередження про потенційні проблеми
- **ERROR** - Помилки, які не зупиняють роботу
- **CRITICAL** - Критичні помилки

### SonarCloud

Проєкт інтегрований з SonarCloud для аналізу якості коду:

- [Дашборд проєкту](https://sonarcloud.io/summary/new_code?id=mplay89_botllm)
- Автоматичний аналіз при кожному Push
- Метрики: Bugs, Vulnerabilities, Code Smells, Coverage

## 🔒 Безпека

### Конфіденційні дані

- Всі API ключі та токени зберігаються в `.env` (не комітяться в Git)
- Використовується `cryptography` для шифрування чутливих даних
- PostgreSQL з'єднання через SSL

### Best Practices

- ✅ `.env` файл у `.gitignore`
- ✅ `.encryption_key` у `.gitignore`
- ✅ Використання змінних оточення
- ✅ Валідація вхідних даних через Pydantic
- ✅ Обмеження прав доступу (Owner → Admin → User)

## 🐛 Відомі проблеми

- Vosk може повільно завантажуватись при першому запуску (завантажує модель)
- Edge-TTS вимагає інтернет-з'єднання для синтезу мовлення

## 🤝 Внесок

Внески вітаються! Будь ласка:

1. Fork проєкту
2. Створіть гілку: `git checkout -b feature/amazing-feature`
3. Закомітьте зміни: `git commit -m 'Add amazing feature'`
4. Push до гілки: `git push origin feature/amazing-feature`
5. Відкрийте Pull Request

## 📝 Ліцензія

Цей проєкт ліцензовано під MIT License - дивіться файл [LICENSE](LICENSE) для деталей.

## 👤 Автор

- GitHub: [@mplay89](https://github.com/mplay89)
- Проєкт: [botllm](https://github.com/mplay89/botllm)

## 📞 Підтримка

Якщо у вас виникли питання або проблеми:

1. Перевірте [Issues](https://github.com/mplay89/botllm/issues)
2. Створіть новий Issue з детальним описом
3. Приєднуйтесь до обговорення в [Discussions](https://github.com/mplay89/botllm/discussions)

## 🙏 Подяки

- [Aiogram](https://docs.aiogram.dev/) - Чудовий фреймворк для Telegram ботів
- [Google Gemini](https://ai.google.dev/) - Потужна AI модель
- [Neon.tech](https://neon.tech/) - Безкоштовний PostgreSQL хостинг
- [SonarCloud](https://sonarcloud.io/) - Аналіз якості коду

---

⭐ Якщо цей проєкт був корисним, поставте зірку на GitHub!
